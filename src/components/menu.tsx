import React from 'react';

/**
 * menu is unlikely to need state so a functional/stateless component should do fine for now
 */ 
const Menu = () =>{
    return (<div className="App-body">This is menu content</div>);
}

export default Menu;